version: '3.8'
services:
  react:
    build:
      context: .
      dockerfile: Dockerfile.react_server
    container_name: rank-tracker-server
    volumes:
      - .:/code
      - node_modules:/code/node_modules  # store the node modules in a volume
    ports:
      - "3000:3000"
    depends_on:
      - flask

  flask:
    build:
      context: .
      dockerfile: Dockerfile.flask
    container_name: flask-serp-api
    volumes:
      - .:/code
    ports:
      - "5000:5000"

volumes:
  node_modules: